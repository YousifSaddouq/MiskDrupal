name: Media Header
props:
  type: object
  properties:

    title:
      type: string
      title: Title
    
    image:
      title: Image
      $ref: json-schema-definitions://canvas.module/stream-wrapper-image-uri